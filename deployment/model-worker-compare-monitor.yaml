apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: fastchat-model-worker1
  labels:
    service-monitor: fastchat-model-worker1
spec:
  selector:
    matchLabels:
      app: fastchat-model-worker1
  endpoints:
  - port: http
    interval: 15s
